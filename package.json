{
  "name": "dsponsor-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph codegen && graph build",
    "codegen": "graph codegen",
    "deploy-sepolia": "graph deploy --network sepolia --studio dsponsor-sepolia",
    "deploy-base-sepolia": "graph deploy --network base-sepolia --studio dsponsor-base-sepolia ",
    "test": "graph test -d"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.3",
    "@graphprotocol/graph-ts": "0.32.0",
    "assemblyscript-regex": "^1.6.4"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
