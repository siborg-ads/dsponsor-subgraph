{
  "name": "dsponsor-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph codegen && graph build",
    "deploy-sepolia": "graph deploy --network sepolia --studio dsponsor-sepolia",
    "test": "graph test -d"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.3",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}